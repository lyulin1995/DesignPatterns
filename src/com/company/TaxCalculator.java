package com.company;

public interface TaxCalculator {
    // It is a contract that specifies the methods that our calculator classes should implement.
    float calculateTax() ;
    // the method doesn't have implementation.
}
